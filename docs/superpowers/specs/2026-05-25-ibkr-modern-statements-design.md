# IBKR Modern Statements — Design Spec

**Date:** 2026-05-25  
**Status:** Approved

---

## Overview

A purely client-side static web app that accepts an IBKR Activity Statement CSV, parses it in the browser, and renders a modern financial dashboard with shareable per-ticker cards. No backend, no data leaves the user's machine.

---

## Tech Stack

| Layer | Choice |
|-------|--------|
| Bundler | Vite |
| UI Framework | React 18 + TypeScript |
| Component Library | Base UI (MUI headless) |
| Styling | Tailwind CSS |
| CSV Parsing | PapaParse |
| Share Image Generation | html2canvas |
| Routing | React Router v6 (hash mode) |
| Charts | Recharts |

---

## Architecture

### Data Flow

```
CSV File Upload
  → PapaParse (raw rows)
  → parseStatement() in lib/parser.ts
  → StatementData (typed structure)
  → StatementContext (React Context)
  → Tab components consume data
```

### File Structure

```
src/
  components/
    upload/         ← UploadZone
    overview/       ← SummaryCards, PieChart
    positions/      ← PositionsTable, ShareModal, ShareCard
    trades/         ← TradesTable, TickerFilter
    ui/             ← shared primitives (Badge, Card, etc.)
  hooks/
    useStatement.ts ← consume StatementContext
  lib/
    parser.ts       ← CSV → StatementData
    calculations.ts ← derived metrics
    shareCard.ts    ← html2canvas export logic
  types/
    statement.ts    ← all TypeScript interfaces
  pages/
    UploadPage.tsx
    DashboardPage.tsx
  App.tsx
  main.tsx
```

Max 8 files per directory. Any directory approaching limit gets split into subdirectories.

---

## Pages

### Upload Page (`/`)

- Full-screen centered layout
- Drag-and-drop zone + click-to-browse
- On file select: parse CSV → if valid, navigate to `#/dashboard`
- On parse error: inline error message with guidance
- Shows "How to export from IBKR" hint (collapsible)

### Dashboard Page (`#/dashboard`)

Three tabs using Base UI Tabs component:

1. **总览 Overview**
2. **持仓 Positions**
3. **交易记录 Trades**

Top bar: account name, report period, language toggle (EN / 中文), theme toggle (light/dark).

---

## Tab Designs

### Tab 1 — Overview

**4 Summary Cards (top row):**
- Current Total NAV (账户总市值)
- Total P/L Amount (总盈亏金额 = Realized + Unrealized)
- Total Return % (总收益率)
- Time-Weighted Return % (时间加权收益率, read directly from CSV)

All cards show green/red coloring based on positive/negative value.

**Portfolio Pie Chart (middle):**
- Donut chart of current holdings by market value
- Legend lists each ticker with % weight

**Period Info (bottom):**
- Report period dates, account ID (masked), base currency

---

### Tab 2 — Positions

Sortable table with columns:

| Ticker | Qty | Cost Price | Current Price | Market Value | Realized P/L | Unrealized P/L | Total P/L | Return % | Action |
|--------|-----|-----------|---------------|-------------|-------------|----------------|-----------|----------|--------|

- Realized P/L, Unrealized P/L, Total P/L, Return %: green if positive, red if negative
- Action column: "分享 Share" button → opens Share Modal

**Share Modal:**
- Two tab cards: "收益率" (Rate Card) | "收益金额" (Amount Card)
- Theme toggle inside modal: Dark | Light
- Download button: exports current visible card as PNG (375×500px)

---

### Tab 3 — Trades

Chronological trade log, newest first.

Columns: Date | Ticker | Side (Buy/Sell) | Qty | Price | Proceeds | Commission | Realized P/L

Ticker filter dropdown (multi-select) above table.

---

## Share Cards

### Card 1 — Rate Card (收益率卡)

```
┌─────────────────────────────┐
│  IBKR        Jan–May 2026   │
│                             │
│  NVDA                       │
│  累计收益率 / Total Return    │
│                             │
│      +33.23%    🚀          │
│                             │
│  成本价 / Cost    $186.50    │
│  当前价 / Price   $215.33    │
│                             │
│  Generated by IBKR Modern   │
└─────────────────────────────┘
```

### Card 2 — Amount Card (收益金额卡)

```
┌─────────────────────────────┐
│  IBKR        Jan–May 2026   │
│                             │
│  NVDA                       │
│  累计盈亏 / Total P/L         │
│                             │
│      +$3,172.29   📈        │
│                             │
│  已实现 / Realized   $0.00   │
│  未实现 / Unrealized $3,172  │
│  总市值 / Mkt Value  $23,686 │
│                             │
│  Generated by IBKR Modern   │
└─────────────────────────────┘
```

**Dark theme:** black background (`#0a0a0a`), neon green (`#00ff88`) for positive, red (`#ff4444`) for negative, white text  
**Light theme:** white background, deep green (`#16a34a`) / deep red (`#dc2626`), dark text — Bloomberg style

Card size: 375×500px logical, exported at 2× resolution (750×1000px PNG).

---

## CSV Parsing Strategy

The IBKR CSV has a multi-section format where each section is identified by the first column. Key sections to parse:

| Section | Data Extracted |
|---------|---------------|
| `Statement` | Period, broker name |
| `Account Information` | Account name, ID, base currency |
| `Net Asset Value` | Current total, prior total |
| `Net Asset Value` (TWR row) | Time-weighted return % |
| `Change in NAV` | Starting value, ending value |
| `Realized & Unrealized Performance Summary` | Per-ticker realized/unrealized P/L |
| `Open Positions` | Per-ticker qty, cost price, current price, market value, unrealized P/L |
| `Trades` | Individual trade rows |
| `Mark-to-Market Performance Summary` | Per-ticker MTM P/L |

Parser reads rows sequentially, switches section context on `Header` rows, accumulates `Data` rows per section.

---

## Internationalization

Language toggle (EN / 中文) stored in React state (no persistence needed).

All user-facing strings wrapped in a simple `t(key)` helper that looks up from a flat `{ en, zh }` dictionary — no i18n library needed given the scope.

---

## Derived Calculations

```
totalNav         = Net Asset Value → Current Total
totalRealizedPL  = sum of Realized Total across all tickers
totalUnrealizedPL = sum of Unrealized Total across all tickers
totalPL          = totalRealizedPL + totalUnrealizedPL
totalReturnPct   = totalPL / (totalNav - totalPL) * 100
twr              = read directly from CSV "Time Weighted Rate of Return" row

perTicker.returnPct = (totalPL) / costBasis * 100
  where costBasis = Open Positions → Cost Basis
  (for fully closed positions: Trades SubTotal → abs(Basis))
```

---

## Non-Goals

- No server, no data persistence, no authentication
- No real-time price updates
- No multi-file / multi-account merging
- No mobile-optimized layout (desktop-first, share cards are mobile-sized by design)
